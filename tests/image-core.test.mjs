import test from'node:test';import assert from'node:assert/strict';import{outputDimensions,drawPlan,safeName,percentDelta,makeZip,PRESETS}from'../public/assets/image-core.js';
test('locked dimensions preserve ratio',()=>assert.deepEqual(outputDimensions(4000,3000,{width:800,height:10,lock:true}),{width:800,height:600}));test('percentage and scale modes',()=>{assert.deepEqual(outputDimensions(800,600,{mode:'percentage',percent:50}),{width:400,height:300});assert.deepEqual(outputDimensions(800,600,{mode:'scale',scale:2}),{width:1600,height:1200})});test('invalid and dangerous dimensions reject',()=>{assert.throws(()=>outputDimensions(0,20,{}));assert.throws(()=>outputDimensions(20000,20000,{width:20000,height:20000,lock:false}))});test('fit and cover plans are centered',()=>{const fit=drawPlan(400,200,200,200,'fit');assert.equal(fit.dy,50);assert.equal(fit.dw,200);const cover=drawPlan(400,200,200,200,'cover');assert.equal(cover.sx,100);assert.equal(cover.sw,200)});test('filename sanitization and delta',()=>{assert.equal(safeName('../bad name.jpg','-small','webp'),'..-bad-name-small.webp');assert.equal(percentDelta(100,75),-25)});test('social presets are exact',()=>assert.deepEqual(PRESETS.youtubeThumbnail,[1280,720]));test('zip writer emits valid signatures',async()=>{const b=makeZip([{name:'a.txt',bytes:new TextEncoder().encode('a')}]),u=new Uint8Array(await b.arrayBuffer());assert.deepEqual([...u.slice(0,4)],[80,75,3,4]);assert.deepEqual([...u.slice(-22,-18)],[80,75,5,6])});
