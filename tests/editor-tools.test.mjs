import test from'node:test';import assert from'node:assert/strict';import{createResume,normalizeResume,moveItem,matchJob,atsAudit,toPlainText,safeFileName}from'../public/assets/resume-core.js';import{normalizeRect,validateSize,rotatedSize,outputInfo,canvasFilter,pixelatePixels,History}from'../public/assets/screenshot-core.js';
test('resume normalization and ordering are deterministic',()=>{const r=normalizeResume({basics:{name:'A'},order:['skills','skills']});assert.equal(r.basics.name,'A');assert.deepEqual(r.order,['skills','summary','experience','education','projects']);assert.deepEqual(moveItem(['a','b','c'],1,-1),['b','a','c'])});
test('ATS audit and matcher report real document signals',()=>{const r=createResume();Object.assign(r.basics,{name:'A',email:'a@b.com',phone:'123',summary:'Engineer delivering measurable accessible web products with reliable browser-first architecture and careful testing.',website:'https://example.com'});r.experience=[{title:'Engineer',organization:'Example',bullets:'Improved speed 35%\nBuilt accessible JavaScript interfaces'}];r.skills=['JavaScript','Accessibility','Testing','HTML','CSS'];const audit=atsAudit(r),match=matchJob(r,'JavaScript engineer accessibility testing Kubernetes');assert.ok(audit.score>=75);assert.ok(match.score>0&&match.missing.includes('kubernetes'));assert.match(toPlainText(r),/EXPERIENCE/);assert.equal(safeFileName(' A / Resume '),'A-Resume')});
test('screenshot geometry and export validation',()=>{assert.deepEqual(normalizeRect({x:8,y:9},{x:-2,y:3},{width:10,height:10}),{x:0,y:3,width:8,height:6});assert.deepEqual(rotatedSize(100,50,90),{width:50,height:100});assert.deepEqual(validateSize(10.2,5.8),{width:10,height:6});assert.deepEqual(outputInfo('jpeg'),{mime:'image/jpeg',extension:'jpg'});assert.match(canvasFilter({brightness:110}),/brightness\(110%\)/)});
test('pixelation and bounded history are functional',()=>{const p=new Uint8ClampedArray([1,2,3,255,9,8,7,255,6,5,4,255,3,2,1,255]);pixelatePixels(p,2,2,{x:0,y:0,width:2,height:2},2);assert.deepEqual([...p],[1,2,3,255,1,2,3,255,1,2,3,255,1,2,3,255]);const h=new History(2);h.push('a');h.push('b');h.push('c');assert.equal(h.undo('d'),'c');assert.equal(h.redo('c'),'d')});
